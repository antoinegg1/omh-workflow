# Run artifacts / candidate solution snapshots — kept locally, not tracked.
# (Contains Anthropic take-home solutions that must not be redistributed.)
backups/
**/backups/

# OS / editor noise
.DS_Store

# OMH runtime output from freeze/test runs inside this repo
workflow-output/
