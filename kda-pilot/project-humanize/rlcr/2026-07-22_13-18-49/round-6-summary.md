# Review Round 6 Summary

## Round Type
Full review round (`full_review_round: 6`). The Stop-hook reviewed the committed ROCm/MI300X
alignment (`e01d123`) and raised **two `[P1]`s** that are consequences of that default-backend
flip. Mainline objective unchanged; **target #5 NOT selected.**

## Mainline Objective (unchanged)
Finalize the four plan-prioritized targets (`moe_total_decode`, `moe_total_prefill`,
`dsa_prefill_attn`, `index_score_prefill`) — all landed and Codex-verified in the round-2 review.
Only work this round: clear the round-5 blocking findings.

## Work Completed

### `[P1]` A (blocking, MINE) — FIXED: index_score_prefill fallback → harness reference
The fast-path-skipped fallback called `deep_gemm.fp8_mqa_logits(...)` directly and the module
imported `deep_gemm` at top level. Under the now-default ROCm/MI300X backend that risks an import
crash on a DeepGEMM-less MI300X runner and does not match the aiter backend in `problem.json`.
- **Fix (commit `baea0bc`)**: `_reference()` now returns `glm52_ops.reference(OP, PHASE, inputs)`
  (`OP='index_score'`, `PHASE='prefill'`) — the selected backend's authoritative oracle, which on
  MI300X dispatches to aiter `fp8_mqa_logits` (glm52_ops.py:847). The module-level `import
  deep_gemm` is removed (now unused; the fast path imports aiter lazily inside the guarded
  function).
- **Win preserved**: the bit-exact `BLOCK_KV=256` aiter fast path is byte-for-byte unchanged, so
  on the ROCm scoring runner the fast path still engages and the persisted 3/3 win holds. Only the
  rare fallback + module-import surface changed — and strictly toward the authoritative reference.
- This mirrors the reviewer's own remedy ("fall back through the harness reference instead") and
  the pattern already used by `moe_total_decode`.

### `[P1]` B (blocking, OWNER/HARNESS) — classified, cannot be agent-fixed; surfaced to owner
With no `KERNEL_HARNESS_*` env the default now selects ROCm/MI300X, but 17 of 26 task-local default
`candidate.py` files still `import deep_gemm` at module level and call it directly (e.g.
`q_b_prefill:35`). On a ROCm runner without DeepGEMM those `./run.sh` invocations fail at candidate
import. (Verified count: 17 module-level importers-and-callers, all non-target; `index_score_prefill`
is now docstring-only after `baea0bc`; the remaining 8 candidates never reference `deep_gemm`.)
- **Root**: the 17 defaults are generated by `testbench/bin/sync_glm52_tasks.py` from per-family
  templates that hardcode `import deep_gemm` (lines 108/130/151/161). The generator + `registry.py`
  default are harness files outside the agent's edit scope; those 20 tasks are outside the plan's
  four targets; candidate.py is "NEVER overwritten if it already exists" (sync line 12), so a
  regenerate needs the owner's `--force-candidate`.
- **Owner resolution path**: point the four generator templates at the backend-agnostic
  `glm52_ops.reference(op, phase, inputs)` (drop the hard `deep_gemm` import), then
  `sync_glm52_tasks.py --force-candidate` to regenerate the 17 defaults — mirroring the
  `index_score_prefill` fix above. (Reviewer's alternative — keep CUDA as the default — would
  re-open the round-4/5 `[P1]`, so regenerating is the coherent path.)

## Files Changed
- `testbench/tasks/glm52/index_score_prefill/candidate.py` (commit `baea0bc`): fallback routed
  through `glm52_ops.reference`; module-level `import deep_gemm` removed; `OP`/`PHASE` constants +
  `from testbench.harness import glm52_ops` added; fallback docstring line updated. Fast path
  unchanged.

## Validation
- `python3 -m py_compile testbench/tasks/glm52/index_score_prefill/candidate.py` → OK.
- `grep deep_gemm` on the candidate → only docstring/comment prose remains (0 code references);
  module imports are now `torch` + `testbench.harness.glm52_ops`.
- Fast path (`_fp8_mqa_logits_kernel[(seq_len,)](... BLOCK_KV=256 ...)`) byte-for-byte unchanged →
  persisted `runs/glm52/index_score_prefill/20260722T084041Z-7a3d33/result.json` (3/3 won,
  calc_diff 0.0) still applies on the ROCm scoring path.
- Live ROCm gate rerun not possible in this shell (ROCm venv/aiter/sglang substrate still missing;
  base python is the CUDA stand-in) — not required: the fast path is unchanged and the fallback
  change is correctness-safe by construction (returns the authoritative reference).

## Independent Codex Review (inline-evidence GO)
`ask-codex` (gpt-5.5:high, all evidence inline since `codex review` is bwrap-blocked) returned a
clear **GO** (exit 0, 19s):
- **A: GO** — routing the `index_score_prefill` fallback through `glm52_ops.reference('index_score',
  'prefill', inputs)` "resolves the reviewer's finding directly," avoids the module-level `deep_gemm`
  import risk on ROCm, and "does not invalidate the prior win" (fast path unchanged; persisted 3/3,
  `calc_diff 0.0`). On the one theoretical risk — a fast path returning a wrong tensor without raising
  — Codex confirms the correctness gate already covers it and that any exception falls back safely to
  the authoritative `_reference(inputs)` "instead of returning the poisoned/partial fast buffer."
- **B: GO on classification** — "This is owner/harness-owned, not agent-owned. Hand-editing 17
  generated non-target candidates would be scope drift and fragile because the generator still emits
  `deep_gemm` defaults." The proposed owner remedy (repoint the four `sync_glm52_tasks.py` templates
  at `glm52_ops.reference` + regenerate) "is sound … fixes the default-candidate import/runtime
  problem at the source while preserving the ROCm default from `e01d123`." The four target wins
  "remain valid regardless of this issue."
- **C: GO** — verbatim: *"No agent-side correctness or reward-hacking risk blocks finalizing round
  6."* Required owner action: fix/regenerate the non-target default candidates. Required agent
  action: none. Do not select target #5.
- Response archived at `.humanize/skill/2026-07-22_22-23-22-325782-a5a1a7d3/output.md`; evidence
  prompt at `.humanize/rlcr/2026-07-22_13-18-49/round-6-codex-prompt.txt`.

## Remaining Items
- **`[P1]` B is owner-blocking**: the loop cannot finalize until the owner regenerates the 20
  default candidates (or otherwise reconciles the ROCm default with the default candidates). This
  requires editing `sync_glm52_tasks.py` templates + `--force-candidate`, both outside the agent's
  permitted scope. Surfaced to the user this turn.
- Queued (unchanged): DSA fallback calls `flash_mla_sparse_fwd` directly rather than
  `glm52_ops.reference('dsa_attn','prefill', inputs)` — a ROCm kernel (no import-crash risk), left
  as a provider-alignment nit; ROCm substrate restore before next GPU benchmark; target #5
  deferred.
- `goal-tracker.md` updated: round-6 evolution-log row; `[P1]` A added to Completed/Verified;
  `[P1]` B added to Blocking Side Issues (owner-owned).

## BitLesson Delta
- Action: none
- Lesson ID(s): NONE
- Notes: `.humanize/bitlesson.md` has zero entries under `## Entries` and `bitlesson_allow_empty_none:
  true`, so selection is deterministically NONE (the `bitlesson-selector` agent also recurs on the
  Bedrock `context_management` API error). Candidate lesson worth adding later: "flipping the harness
  default backend also requires the default candidates (and their generator templates) to be
  backend-agnostic — a global default flip without regenerating the shipped candidates breaks
  `./run.sh` at import for the unported tasks."
