# Ask Codex Input

## Question

Adjudicate a backend-authority contradiction in a Humanize RLCR loop and give a clear GO or NO-GO on the current candidate state. Be concrete about whether the round-4 finding is a real candidate defect or a stale-metadata false positive.

SETTING
- Repo kernel-harness-amd. GLM-5.2 kernels on AMD MI300X / ROCm gfx942. The ONLY correctness+performance authority is the frozen taskset file tasksets/glm52_rocm_local.json plus the roofline_mfu_bw evaluator. The loop is invoked with KERNEL_HARNESS_PLATFORM=rocm KERNEL_HARNESS_PROFILE=amd-mi300x.
- I may edit ONLY the sanctioned candidate.py files. I may NOT edit oracle/generated files: task.json, glm52_ops.py, evaluate_task.py, the taskset, or any harness/reference file. I may NOT fabricate a benchmark or weaken tolerances.

THE CONTRADICTION BETWEEN TWO CONSECUTIVE CODE REVIEWS
- Round 3 review [P1]: dsa_prefill_attn/candidate.py took a PyTorch fp32-QK sparse-MLA reimplementation for every valid input. It said: on a CUDA/B200 build the reference flash_mla_sparse_fwd IS the fast CUDA FlashMLA kernel, so the heavier PyTorch path would REGRESS the gate; add a platform guard so the fast path only engages where the reference is slow. I fixed it: added `if torch.version.hip is None: raise` so run()s try/except falls back to the reference on non-ROCm.
- Round 4 review [P2] (two findings) now flags the OPPOSITE: (a) dsa guard `torch.version.hip is None` and (b) index_score_prefill guard `if _mqa_mod.arch != "gfx942": raise`. It argues: the two candidates committed task.json say `deployment: B200-DP1-TP1-EP32`, so on the committed B200 task the guards always raise and always fall back to the reference, so the candidates can never beat the reference on their documented task; unreachable fast path.

EVIDENCE THE AUTHORITATIVE GATE IS ROCm, NOT B200
1. tasksets/glm52_rocm_local.json (the frozen authority named as the ONLY gate) has: hardware = {platform: rocm, profile: amd-mi300x, provider: aiter-torch-reference, timer: event}. Its score_model.official_metrics = [dsa_prefill_attn, index_score_prefill, moe_total_prefill, moe_total_decode]. So dsa and index_score ARE scored, on ROCm/MI300X, by this taskset.
2. task.json deployment strings are INCONSISTENT with each other: moe_total_decode and moe_total_prefill read MI300X-DP1-TP1-EP32, but dsa_prefill_attn and index_score_prefill still read B200-DP1-TP1-EP32. These task.json files are generated by testbench/bin/sync_glm52_tasks.py; the B200 strings are stale leftovers from pre-ROCm authoring. I am forbidden to edit them.
3. The authoritative persisted result.json for dsa (runs/glm52/dsa_prefill_attn/20260722T083802Z-1b233d/result.json) backend block = {platform: rocm, profile: amd-mi300x, accelerator: AMD MI300X}, environment.gpu_arch = gfx942:sramecc+:xnack-. per_shape = 3/3 shapes won, worst calc_diff 2.884e-6 <= 5e-6, 0 regress. index_score result.json = 3/3 won, calc_diff 0.0 bit-exact.
4. The runtime guards key off the ACTUAL device (torch.version.hip is set on the ROCm run; _mqa_mod.arch == gfx942 on MI300X), NOT off the stale task.json string. So on the authoritative ROCm gate BOTH guards pass and the fast paths engage and win.

WHY NO CANDIDATE-ONLY EDIT CAN SATISFY THE B200 PREMISE
- If the premise were truly B200: guarded => neutral fallback (round-4 complaint); unguarded => the round-3 [P1] regression (PyTorch loop slower than fast CUDA FlashMLA). Winning on B200 would require a genuinely faster-than-FlashMLA B200 kernel, which needs B200 hardware I do not have and the plan explicitly forbids B200 assumptions. And I cannot edit task.json deployment metadata (forbidden oracle file).

WHAT I CHANGED THIS ROUND (doc-only, no logic change)
- Added comments next to both guards citing the authoritative taskset (platform: rocm, official_metrics lists both tasks) and noting the task.json B200 string is stale metadata I cannot edit. No control-flow or math change. py_compile OK. The ROCm fast paths are byte-for-byte unchanged, so the persisted 3/3 ROCm wins still hold.

QUESTIONS
1. Given the frozen taskset pins platform=rocm and enumerates both tasks as official_metrics, is keeping the runtime device guards the CORRECT engineering choice, and is the round-4 [P2] premise (must win on the B200 task.json deployment) a stale-metadata false positive rather than a candidate defect?
2. Is there any reward-hack or correctness risk in the doc-only comment change or in keeping the guards?
3. Is the right disposition: keep guards, classify the [P2] as an owner-facing stale-task.json-metadata issue (queued, since task.json is a forbidden edit), and finalize on the ROCm authority? Or is there a permitted candidate-side action I am missing?
Give a clear GO or NO-GO for committing this doc-only change and finalizing on the ROCm gate.

## Configuration

- Model: gpt-5.5
- Effort: xhigh
- Timeout: 5400s
- Timestamp: 2026-07-22_18-25-11
- Tool: codex
