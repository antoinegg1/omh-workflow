# Legacy plan chain

This directory archives the inactive draft -> plan review -> revise -> finalize -> implement workflow chain and its tests.

The active workflow uses one PlanImplement agent followed by independent functional review and bounded rework. Files here are retained only for history and comparison. Tests use the `.disabled` suffix so the active Bun test suite does not discover them.
